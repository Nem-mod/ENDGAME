#include "../inc/window_sdl.h"

#include <stdbool.h>
void mx_init_scene(SDL_Event event, t_window_sdl* gameWindow) {
    switch (gameWindow->scene)
    {
    case MENU: {

        break;
    }
    case MAP: {
        break;
    }
    case LEVEL: {
        break;

    }
}